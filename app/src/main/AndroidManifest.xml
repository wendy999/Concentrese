<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.platzi.concentrese">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".InicioActivity" />
        <activity android:name=".NickNameActivity" />
        <activity android:name=".opciones.TipoJuegoActivity" />
        <activity android:name=".opciones.ModoJuegoActivity" />
        <activity android:name=".opciones.PuntageAltoActivity" />
        <activity android:name=".opciones.JuegoFacilActivity" />
        <activity android:name=".opciones.JuegoMedioActivity" />
        <activity android:name=".opciones.JuegoDificilActivity">
        </activity>
    </application>

</manifest>